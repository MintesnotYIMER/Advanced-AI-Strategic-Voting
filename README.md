# Surrogate Modeling of Strategic Voting Dynamics at National Scale

## Project Overview
This repository contains the final project for the Advanced Topics in AI (ATAI) course at Université Toulouse Capitole. The project explores the effects of strategic voting in a multi-candidate election (based on the 12 candidates of the 2022 French Presidential Election) using Agent-Based Modeling (ABM) and Deep Learning. 

Our goal is to model how voters rationally abandon their sincere preferences for a candidate with a better chance of winning, driven by polling information shared across a social network. To overcome the computational limits of running ABMs for large populations, we built a Deep Learning surrogate model capable of predicting election dynamics at a national scale.

**Authors:** Mintesnot Nigusu Yimer, Hritik Bikram Rawal, Florence Angel Ayyala

---

## Repository Structure & Methodology

The repository is structured to reflect the four main phases of our methodology:

### Part 1: Data Generation (`/1_Data_Generation`)
* **`part1_preferences_and_network.py`**: A Python script using `preflibtools` and `networkx` to generate the initial conditions for the simulation. It generates voter preferences using Impartial Culture (IC) and Mallows models, and constructs voter social networks using Erdős-Rényi (ER) and Barabási-Albert (BA) graphs for populations of 1,000, 3,000, and 5,000 agents.

### Part 2: Agent-Based Simulation (`/2_Simulation`)
* **`election.gaml`**: The GAMA platform simulation code. It simulates a 60-day election cycle where agents react to daily polls based on their Nash equilibrium calculations. The model tracks social welfare, the variance of the candidate scores, and the number of strategic opinion changes per day. It includes three types of agents: Stubborn, Strategic, and Mixed.

### Part 3: Deep Learning Surrogate Model (`/3_Surrogate_Model`)
* **`Part3.ipynb`**: A Jupyter Notebook containing the data preprocessing and the TensorFlow/Keras implementation of a Bidirectional Long Short-Term Memory (BiLSTM) neural network. The surrogate model is trained on the time-series data generated by GAMA to rapidly predict daily vote changes, allowing us to extrapolate the simulation to a population of 100,000 voters.

### Part 4: Analysis & Reporting (`/4_Analysis_and_Report`)
* **`plotforGamaResult.ipynb`**: A Jupyter Notebook used to plot and visualize the outputs of the GAMA simulations and compare them against the surrogate model's predictions.
* **`Report.pdf`**: The final academic paper formatted in the ACM double-column template, detailing the game theory formalization, simulation setup, surrogate model architecture, and discussions on scalability.

---

## Prerequisites & Installation

1. **Python 3.x:** Required for data generation and Jupyter Notebooks.
2. **GAMA Platform:** Required to run the Agent-Based Model (`election.gaml`).
3. **Required Python Libraries:**
   ```bash
   pip install pandas numpy matplotlib seaborn scikit-learn tensorflow joblib preflibtools networkx
